import React from 'react'
import {connect} from 'react-redux'
const Module  = (props)=>{
    return <div>Hi I am a example</div>
}
const mapStateToProps = state=>({

})
const mapDispachtoProps = {

}
export default connect(mapStateToProps, mapDispachtoProps)(Module)